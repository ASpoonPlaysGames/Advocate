name: format-check

on: pull_request

jobs:
  check-format:
    runs-on: windows-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2
      
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'

      - name: Install dotnet-format tool
        run: dotnet tool install -g dotnet-format
        
      - name: Run dotnet format
        id: format
        continue-on-error: true 
        run: dotnet format 'Advocate.sln' --verbosity diagnostic --verify-no-changes

      - name: Get diffs
        run: git diff b2754a9af0edf2e61482fdb5d330afbeee88fa5d
  
      